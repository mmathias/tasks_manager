<%= form_for task, url: url, html: {class: "task-form"}  do |f| %>
  <p>
    <%= f.label :title, t("labels.task.title") %>
    <%= f.text_field :title, maxlength: 255, size: 100 %>
    <%= f.submit class: "button" %>
  </p>
<% end %>
